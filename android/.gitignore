*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build/

# Signing key material - shared between local builds and CI via a GitHub
# Actions secret (see .github/workflows/android-release.yml), never
# committed. The repo is public; a signing certificate sitting in git
# history would let anyone forge an "update" with the same identity.
keystore/*.keystore
