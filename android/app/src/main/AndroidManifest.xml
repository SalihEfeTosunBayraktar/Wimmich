<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Reading the device's own photos/videos to back them up - READ_MEDIA_* on
         Android 13+ (API 33), READ_EXTERNAL_STORAGE below that. Both declared so
         the app installs correctly across the minSdk 26 - target 34 range. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Self-update: lets this app prompt the user to install a downloaded
         APK (see update/UpdateInstaller.kt). Android still requires the
         user's explicit tap-through on the system installer either way -
         this permission only unlocks that installer from being invoked by
         a non-Play-Store app at all. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".WimmichApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="Wimmich"
        android:theme="@style/Theme.Wimmich"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Wimmich">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Grants the system package installer temporary read access to a
             downloaded APK sitting in this app's private cache dir - the
             installer runs as a different app/process and can't otherwise
             read our cacheDir. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
