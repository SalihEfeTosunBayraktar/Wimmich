# Test-only dependencies. Deliberately NOT in requirements.txt: an end user
# installing Wimmich has no reason to pull a test runner, and install_*.bat
# stays as small as it can be.
#
#   pip install -r requirements-dev.txt
#   pytest
pytest>=8.3.0
pytest-asyncio>=0.24.0

# Drives the ASGI app in-process via httpx.ASGITransport - no uvicorn, no
# port, no lifespan. That last part matters: running the real lifespan would
# start the background job worker, shell out to check FFmpeg, and open a
# browser tab, none of which a test suite wants.
httpx>=0.27.0
